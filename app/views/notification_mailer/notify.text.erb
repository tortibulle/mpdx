<% @notifications_by_type.each do |type, notifications|
     contacts = notifications.collect(&:contact) %>
<%= type.description %>:
<% contacts.sort_by(&:name).each do |contact| %>
* <%= type.task_description(contact) %>
<% end %>

<% end %>
