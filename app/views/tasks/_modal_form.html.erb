<%= form_for(@task || current_account_list.tasks.new, remote: true) do |f| %>
  <%= hidden_field_tag 'add_task_contact_ids' %>
  <%= hidden_field_tag 'bulk_task_update_ids' %>
  <%= render 'tasks/fields', task: f.object, f: f %>
<% end %>

